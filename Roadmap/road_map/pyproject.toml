[project]
name = "road-map"
version = "0.1.0"
description = ""
authors = [
    {name = "sevilkhojasteh",email = "sevilkhojasteh@gmail.com"}
]
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "fastapi (>=0.137.1,<0.138.0)",
    "uvicorn (>=0.49.0,<0.50.0)",
    "numpy (>=2.4.6,<3.0.0)"
]

[tool.poetry]
packages = [{include = "road_map", from = "src"}]

[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"
